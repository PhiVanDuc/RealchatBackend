const createMessage = require("./create-message");
const listMessage = require("./list-message");
const partnerMessage = require("./partner-message");
const readMessage = require("./read-message");
const deleteMessage = require("./delete-message");

module.exports = {
    createMessage,
    listMessage,
    partnerMessage,
    readMessage,
    deleteMessage
}